# EventDhondo - Lab Project

## Setup Instructions
1. Clone the repo.
2. **Frontend:** `cd frontend` then `npm install` then `npm start`.
3. **Backend:** `cd backend` then `npm install` then `node server.js`.
4. **Database:** Scripts are in `/database`.